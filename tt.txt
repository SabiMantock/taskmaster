      <Text style={{ marginBottom: 20 }}>Create task</Text>

      <View>
          {note ? (
            <View style={{ alignItems: 'center' }}>
              <HorizontalLine />
              <TextInput
                placeholder='Enter note...'
                style={{
                  width: 406,
                  height: 70,
                  backgroundColor: '#FAFBFE',
                  padding: 20,
                  borderRadius: 30,
                  textAlignVertical: 'top'
                }}
              />
            </View>
          ) : (
            <Pressable
              onPress={() => setNote(!note)}
              style={{ position: 'absolute', right: 5, top: 20 }}
            >
              <Text>Add note</Text>
            </Pressable>
          )}
        </View>

         style={{
        backgroundColor: '#FAFBFE',
        borderRadius: 30
      }}


duration
      <View
                style={
                  {
                    // borderWidth: 1,
                    // borderRadius: 30,
                    // width: 200,
                    // alignItems: 'center',
                    // textAlign: 'left',
                    // justifyContent: 'center',
                    // borderColor: '#909cc6'
                  }
                }
              >
                <Pressable
                  onPress={() => {
                    setPicker(!picker)
                  }}
                >
                  <Text>Duration</Text>
                  <Text style={{ fontSize: 42 }}>
                    03
                    <Image
                      source={require('../../assets/dot.png')}
                      style={{
                        width: 12,
                        height: 12
                      }}
                    />
                    30
                  </Text>
                </Pressable>
              </View>




                  <CustomModal
      isVisible={scroller}
      animationIn='slideInUp'
      animationOut='slideOutDown'
      backDropOpacity={0.6}
      animationInTiming={500}
      animationOutTiming={500}
      onBackdropPress={() => {
        setScroller(false)
      }}
      style={{ margin: 0, padding: 0, justifyContent: 'flex-end' }}
    >

       </CustomModal>